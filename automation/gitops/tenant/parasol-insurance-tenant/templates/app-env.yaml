{{- $prefix := include "parasol-insurance-tenant.prefix" . -}}
{{- range $env := list "dev" "prod" }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ $prefix }}-{{ $env }}
  namespace: {{ $.Values.argocd.namespace }}
  labels:
    app.kubernetes.io/instance: {{ $prefix }}-parasol-insurance
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  # No finalizer — see comment in app-build.yaml
spec:
  destination:
    namespace: {{ $prefix }}-{{ $env }}
    server: https://kubernetes.default.svc
  project: {{ include "parasol-insurance-tenant.argocd-project" $ }}
  source:
    path: app
    repoURL: https://{{ $.Values.gitlab.host }}/{{ $.Values.tenant.username }}/parasol-insurance-manifests.git
    targetRevision: main
    helm:
      valueFiles:
        - values/values-{{ $env }}.yaml
      valuesObject:
        namespace: {{ $prefix }}-{{ $env }}
        tenant:
          username: {{ $.Values.tenant.username }}
        registry:
          host: {{ $.Values.registry.host }}
          organization: {{ $.Values.tenant.username }}
        kafka:
          topic: {{ $prefix }}-intake
        cluster:
          subdomain: {{ $.Values.cluster.subdomain }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - RespectIgnoreDifferences=true
{{- end }}
